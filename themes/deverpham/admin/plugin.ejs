<%- await include('template/header.ejs') %>
<%- await tableView({

    name: 'Plugin',
    items: plugins,
    fields: [
        'name', 
        'version', 
        'description', 
        'author', 
        'active'],
    addons: [{
        name: 'action',
        each: function(item) {
            return 'gg';
            return link({
                name: 'thinh',
                link: 'thinh'
            })
        }
    }]
}) %>

<%-await include('template/footer.ejs') %>