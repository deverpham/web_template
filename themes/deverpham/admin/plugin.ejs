<%- await include('template/header.ejs') %>
<%- await tableView({

    name: 'Plugin',
    items: plugins,
    fields: [
        'name', 
        'version', 
        'description', 
        'author', 
        'active'],
    addons: [{
        name: 'action',
        each: function(item) {
            return link({
                name: item.active? 'disable' : 'active',
                link: '/admin/plugin/' + item.name
            })
        }
    }]
}) %>

<%-await include('template/footer.ejs') %>