const React = require('react');
const {Crud} = require('./crud');
export class Category extends React.Component {
    render() {
        
        return (
            <div>
                <Crud />
            </div>
        )
    }
}